import PageShell from "@/components/PageShell";

export default function GalleryPage() {
  return (
    <PageShell
      eyebrow="Gallery"
      title="Look around."
      copy="Interiors, amenities, the grounds, the neighborhood. A visual index of everything that makes The 24 what it is."
    />
  );
}
