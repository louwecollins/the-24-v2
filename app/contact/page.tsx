import PageShell from "@/components/PageShell";

export default function ContactPage() {
  return (
    <PageShell
      eyebrow="Contact"
      title="Say hello."
      copy="Tours run daily. Call, email, or fill in the short form. We'll get back within a business day."
    />
  );
}
