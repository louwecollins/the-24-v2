import PageShell from "@/components/PageShell";

export default function CampusPage() {
  return (
    <PageShell
      eyebrow="24 Campus"
      title="Eight acres. One address."
      copy="The 24 sits on its own campus — gardens, courtyards, paths, pools. A map of the grounds, the buildings, and what connects them."
    />
  );
}
