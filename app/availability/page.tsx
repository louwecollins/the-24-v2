import PageShell from "@/components/PageShell";

export default function AvailabilityPage() {
  return (
    <PageShell
      eyebrow="Availability"
      title={`Live floor plans.\nReal-time counts.`}
      copy="Studio to three-bedroom residences across the North and South Towers. Filter by bedroom count, square footage, or move-in window."
    />
  );
}
